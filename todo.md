# TODO

- [x] Update the legal RAG prompt instructions to remove bracket citations while keeping safety constraints.
- [x] Adjust deterministic fallback formatting in `apps/web/lib/legal-rag.ts` to remove `[see: ...]` references.
- [x] Respond to user about document curation vs providing entire constitution.
- [x] Review existing workspace structure to locate where the locker component should live.
- [ ] Implement encrypted locker UI/logic (component, styles, crypto utils, IndexedDB helpers).
- [ ] Provide user with implementation summary and next steps.
